{
  "name": "s-for-serverless",
  "version": "0.0.3",
  "description": "A simple serverless function that handles requests from a frontend app.",
  "repository": "https://github.com/joey-ma/s-for-serverless.git",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "@vercel/node": {
      "path-to-regexp": "8.2.0"
    }
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  }
}
